# Core
# fastapi >= 0.115 also pulls starlette >= 0.40 which clears PYSEC-2026-1943;
# >= 0.124 pulls starlette >= 1.0.1 clearing the rest of the starlette advisories.
fastapi==0.141.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
# PyJWT replaces python-jose (PYSEC-2025-185 has no fixed release; ecdsa
# advisory is unfixable in the 0.19 line). API-compatible for our HS256 use.
PyJWT==2.13.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.32
python-dotenv==1.2.3
# >= 2.9 required by fastapi 0.141; standard v2 API (BaseModel/EmailStr/field_validator)
pydantic==2.12.4
email-validator==2.3.0
aiofiles==23.2.1
# 0.11.x pulls pdfminer.six >= 20251107 (clears PYSEC-2026-1761/1762)
pdfplumber==0.11.10
python-docx==1.1.0
httpx==0.25.2
aiosqlite==0.19.0

# Google OAuth (sign-in + Gmail)
google-auth==2.37.0
google-auth-oauthlib==1.4.1
google-auth-httplib2==0.4.2
google-api-python-client==2.199.0
pycryptodome==3.23.0  # replaces deprecated pycrypto dependency chain of google-api-client where pulled

# Background jobs
APScheduler==3.11.3

# Database
alembic==1.19.1
psycopg2-binary==2.9.10

# Security
cryptography==50.0.0
# 4.0.x: passlib 1.7.4 reads bcrypt.__about__ which was removed in 4.1+
bcrypt==4.0.1

# Caching / rate limiting (optional — in-memory fallback works without it)
redis==5.0.1

# Test dependencies
pytest==9.1.1
